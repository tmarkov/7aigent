The Julia code below is blocked waiting for input.

Julia source:
```julia
{{julia_source}}
```

Elapsed time: {{elapsed_time}} seconds
Output so far:
```
{{output_so_far}}
```

Input prompt: {{prompt}}

Return a JSON object matching this schema:
```json
{{json_schema}}
```
