The Julia code below is still running.

Julia source:
```julia
{{julia_source}}
```

Elapsed time: {{elapsed_time}} seconds
Output so far:
```
{{output_so_far}}
```

Decide whether to continue waiting or interrupt execution. Return a JSON
object matching this schema:
```json
{{json_schema}}
```
