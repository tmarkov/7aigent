{
  "name": "7aigent",
  "version": "0.0.1",
  "private": true,
  "description": "ReACT agent runner for 7aigent codebase exploration",
  "scripts": {
    "build": "spago bundle --bundle-type app --module Main --outfile index.js --platform node",
    "test": "spago test --main Test.Main"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "eventsource-parser": "^3.0.1",
    "smol-toml": "^1.3.4",
    "zeromq": "^6.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
