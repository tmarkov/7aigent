System environment provides project context.

The system section displays the project directory, agent instructions (if present), git status (if a git repository), and the file tree.

This environment has no commands. It is read-only project context updated automatically on each screen refresh.
